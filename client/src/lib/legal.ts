/**
 * Single source of truth for legal-surface contact details.
 *
 * PLACEHOLDER: `legal@example.com` is RFC 2606 reserved and will not receive
 * mail. Replace it with a monitored address before shipping -- the DMCA page
 * promises a contact channel, and an unmonitored one makes that promise
 * worthless. Both `/terms` and `/dmca` read from here so it is a one-line
 * change.
 */
export const LEGAL_CONTACT_EMAIL = "legal@example.com";

/** Date the current version of the legal pages took effect. */
export const LEGAL_EFFECTIVE_DATE = "26 September 2026";
